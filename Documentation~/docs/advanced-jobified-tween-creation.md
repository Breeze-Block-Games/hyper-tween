# Jobified Tween Creation

HyperTween allows for simple creation of tweens from jobs by creating a `TweenFactory` from the `SystemState` as follows:

[!code-csharp[CS](../../Samples~/Examples/Systems/JobifiedTweenExampleSystem.cs)]